image: python:3.10

stages:
  - deploy

publish_to_pypi:
  stage: deploy
  script:
    - pip install build twine
    - python -m build
    - twine upload dist/*
  rules:
    - if: $CI_COMMIT_TAG =~ /^v/