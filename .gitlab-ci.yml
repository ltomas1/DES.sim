image: python:3.10

stages:
  - deploy

publish_to_pypi:
  stage: deploy
  tags:
    - shared_docker
  script:
    - pip install build twine
    - python -m build
    - twine upload dist/*
  rules:
    - if: $CI_COMMIT_TAG =~ /^v/
    - if: $CI_COMMIT_BRANCH == "main"